# Fast MCP Telegram Grafana Dashboards

The authoritative deployment source for Grafana dashboards and datasources is in `vds-servers`:
`/root/vds-servers/apps/services/grafana/`

Dashboards are provisioned on the apps VDS from `vds-servers`. Do not store duplicate dashboard definitions here.
